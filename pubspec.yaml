name: common
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  dart_code_metrics: ^5.7.6 #문제점 파악해주는 툴
  go_router: ^12.1.0 # 라우팅 페이지 이동
  flutter_svg: ^2.0.9 # svg 파일
  flutter_bloc: ^8.1.3
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  dio: ^5.3.3
  retrofit: ^4.0.1
  get_it: ^7.6.0
  logger: ^2.0.2
  mocktail: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  freezed: ^2.4.1
  build_runner: ^2.4.6
  json_serializable: ^6.7.1
  retrofit_generator: ^7.0.8




flutter:
  uses-material-design: true
  ## 이미지들 넣는곳
  assets:
    - assets/
    - assets/svg/
    - assets/icon/
    - assets/image/